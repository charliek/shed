{
  "//": "Expected decode of sample.yaml, sorted by name (both parsers sort machines: entries by name). `broken` is intentionally ABSENT — both decoders skip a malformed entry rather than fail the whole document. `withrc` carries a leftover `rc_bin` key (plus an unknown `color` key) that NEITHER language models any more (C8, plan 022 S6, retired `rc_bin` with `sx`) — this is the tolerance negative control: an unmodeled key must not stop the entry from decoding. See README.md.",
  "entries": [
    {
      "name": "bare",
      "host": "bare",
      "user": null,
      "ssh_port": 22,
      "known_hosts": null
    },
    {
      "name": "full",
      "host": "builder.example.com",
      "user": "charliek",
      "ssh_port": 2022,
      "known_hosts": "/Users/dev/.ssh/known_hosts"
    },
    {
      "name": "withrc",
      "host": "side.example.com",
      "user": null,
      "ssh_port": 22,
      "known_hosts": null
    }
  ]
}
